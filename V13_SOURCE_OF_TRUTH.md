# V13 SOURCE OF TRUTH

В этом проекте Home из `AUREA_UI_home_logo_line_v13` закреплён как визуальный эталон.

Что это значит:

- Home не перерисовывается заново;
- Home не разбирается на новые стилизованные кнопки и тени;
- Home не подменяется «похожим» shell;
- все следующие окна должны опираться на ту же художественную систему: тот же фон, тот же свет, ту же нижнюю навигацию, ту же общую геометрию.

Практически это уже подготовлено:

- исходный точный Home сохранён;
- chrome-слой v13 вынесен в `assets/foundation/chrome/`;
- reference-экраны вынесены в `assets/foundation/reference/`;
- `src/foundation/V13ChromeShell.js` готов как техническая основа для следующих окон.

Следующий шаг: брать этот shell и наполнять **только центральный контент**, не ломая Home.
